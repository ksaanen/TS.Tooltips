{
  "name": "tooltips",
  "version": "1.0.0",
  "description": "Standalone tooltips script",
  "main": "index.js",
  "scripts": {
    "start": "tsc -w",
    "build": "npm run sass:build && tsc -b",
    "sass:build": "node-sass src/tooltips.scss -o dist",
    "sass:watch": "npm run sass:build && node-sass src/tooltips.scss -wo dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^4.12.0",
    "typescript": "^3.6.4"
  }
}
